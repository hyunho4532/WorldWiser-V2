<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="tourSpotsSelectViewModel"
            type="com.hyun.worldwiser.viewmodel.TourSpotsSelectViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="370dp"
        android:background="@color/white"
        android:gravity="center"
        android:minWidth="300dp"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/tv_tour_spot_dialog_main_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="여행 관광지 조회"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_tour_spot_dialog_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:textSize="14sp"
            android:text="@{tourSpotsSelectViewModel.tourSpotsTitle}"
            app:layout_constraintStart_toStartOf="@+id/tv_tour_spot_dialog_main_title"
            app:layout_constraintTop_toBottomOf="@+id/tv_tour_spot_dialog_main_title" />

        <FrameLayout
            android:id="@+id/map"
            android:layout_width="380dp"
            android:layout_height="220dp"
            android:layout_marginTop="16dp"
            app:layout_constraintStart_toStartOf="@+id/tv_tour_spot_dialog_title"
            app:layout_constraintTop_toBottomOf="@+id/tv_tour_spot_dialog_title" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>